services:
  postgres:
    image: postgres:17
    container_name: denguetrace-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: denguetrace
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"